<UserControl x:Class="ZImageTests.GUI.Controls.ImagePane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:elementary="clr-namespace:ZImageTests.GUI.Controls.Elementary" 
             xmlns:local="clr-namespace:ZImageTests.GUI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource ChessboardBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <!--<TextBlock Grid.Row="1"/>-->
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <elementary:ZoomPicker MinWidth="100"/>
        </StackPanel>
    </Grid>
</UserControl>
